public extension VAST.Element {
    /// A HTML code snippet (within a CDATA element).
    ///
    /// A “snippet” of HTML code to be inserted directly within the publisher’s HTML page code.
    typealias HTMLResource = String
}
